/**
 * `/creator/explore` — public discovery surface: browse active Creators and the
 * global leaderboard. Placeholder; no behavior yet.
 */
export default function ExplorePage() {
  return (
    <section className="mx-auto flex w-full max-w-5xl flex-col gap-4 px-6 py-24">
      <h1 className="font-display text-3xl font-semibold tracking-tight">Explore</h1>
      <p className="text-muted-foreground">
        Browse active Creators and the global leaderboard. Placeholder.
      </p>
    </section>
  );
}
