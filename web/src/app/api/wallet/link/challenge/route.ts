import { notImplemented } from "@/app/api/_not-implemented";

/** POST /api/wallet/link/challenge — generate nonce, return human-readable challenge. */
export async function POST() {
  return notImplemented();
}
