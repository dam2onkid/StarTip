/**
 * Reusable trustline onboarding primitives will be exported here.
 *
 * This scaffold deliberately contains no StarTip-specific Donation flow,
 * sponsorship policy, or relayer implementation. Those remain host-app
 * responsibilities behind the package's future public adapter contracts.
 */
export {};
